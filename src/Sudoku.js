"use strict";

class Sudoku {
  /**
   * Preliminary API:
   *
   * - createSudoku(difficulty)
   * - newGame()
   * - restart()
   * - check (number<1to9>, x, y) -> Boolean
   *
   *
   */
}

module.exports = Sudoku;
